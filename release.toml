allow-branch = ["main"]
consolidate-commits = true
consolidate-pushes = true
disable-publish = true
no-dev-version = true
pre-release-commit-message = "chore: release \`{{version}}\` (skip ci)"
pre-release-hook = ["convco", "changelog", ">", "CHANGELOG.md"]
